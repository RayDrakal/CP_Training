#include <bits/stdc++.h>
#define FASTIO ios::sync_with_stdio(false); cin.tie(NULL); cout.tie(NULL);
#define INF 0x3f3f3f3f
using namespace std;
int n, c;
int main() {
	FASTIO;
	cin >> n;
	cout << 1 << '\n' << 0;
}